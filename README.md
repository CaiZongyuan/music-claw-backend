To install dependencies:
```sh
bun install
```

To run:
```sh
bun run dev
```

open http://localhost:3000

Quick checks:
- `GET /` health check
- `GET /verify` simple HTML verifier
- `POST /api/qq/parse` parse QQ share URL
- `POST /api/netease/match` search Netease candidates
